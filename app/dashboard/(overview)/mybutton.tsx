// "use client"

// import { SeedRev } from "@/app/lib/custom-actions"
// import { startTransition, useActionState } from "react"



// export default function MyButton() {
//     const [state, action, pending] = useActionState(SeedRev, null)

// return <button onClick={() => startTransition(action)} style={{color: 'black', border: 'black'}}>Seedear datos</button>
// }